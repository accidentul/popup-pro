# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/exit_intent_popup

# Server Configuration
PORT=3001
NODE_ENV=development
JWT_SECRET=your-secure-random-jwt-secret-here-change-this-in-production

# Shopify OAuth Credentials
# Get these from your Shopify Partner Dashboard: https://partners.shopify.com/
SHOPIFY_API_KEY=your-shopify-api-key
SHOPIFY_API_SECRET=your-shopify-api-secret

# Application URLs
# For development, use localhost
FRONTEND_URL=http://localhost:3000

# For production, use your public domain (must be HTTPS)
# API_BASE_URL=https://your-domain.com
# In development, use localhost (script tag installation will be skipped)
API_BASE_URL=http://localhost:3001

# Database Connection Pool Settings
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# Redis Configuration (optional, for caching)
REDIS_URL=redis://localhost:6379

# Production Deployment Notes:
# 1. Change API_BASE_URL to your public HTTPS domain (e.g., https://api.yourapp.com)
# 2. Update FRONTEND_URL to your production frontend URL
# 3. Use strong, random values for JWT_SECRET
# 4. Never commit the actual .env file to version control
# 5. For Shopify app deployment, you'll need:
#    - A public HTTPS domain
#    - Shopify app configured with correct redirect URLs
#    - Database accessible from your server
